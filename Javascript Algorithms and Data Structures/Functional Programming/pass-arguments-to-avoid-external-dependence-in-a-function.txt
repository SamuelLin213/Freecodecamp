//declare dependencies explicitly: if function depends on var/object, pass it in directly into function as argument 

// The global variable
let fixedValue = 4;

// Only change code below this line
function incrementer(val) {
  return val+1;

  // Only change code above this line
}