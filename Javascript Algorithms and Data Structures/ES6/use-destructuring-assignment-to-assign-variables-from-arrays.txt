/*can easily destructure arrays like objects, difference between spread operator and array destructuring is that spread operator unpacks all content into comma-separated list but you can't choose which elements to assign to variables*/

let a = 8, b = 6;
// Only change code below this line
[a, b] = [b, a];